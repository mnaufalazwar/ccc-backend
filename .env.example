# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Database
DB_URL=jdbc:postgresql://localhost:5433/chitchatclub
DB_USER=postgres
DB_PASS=postgres

# JWT (generate a strong secret for production: openssl rand -base64 64)
JWT_SECRET=myDefaultDevJwtSecretKeyThatIsAtLeast256BitsLongForHS256Algorithm123456

# Email (Resend) — get your API key from https://resend.com
MAIL_PASSWORD=your-resend-api-key-here

# Frontend URL (used in verification and password reset email links)
FRONTEND_URL=http://localhost:5173
